<li class="panel-body">
  <%=post.message%>
  <p>
    <span id="destroy-span">
      <%= link_to 'Destroy', post_path(post.id),
                      method: :delete%>
    </span>
    <span id="date-span">
      <%= link_to post.created_at.strftime('%A %B %d at %H:%M'),post_path(post.id)%>
    </span>
  </p>
</li>
<br/>

