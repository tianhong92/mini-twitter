<li><%= post.message %>
    <%= post.created_at %>
    <%= link_to 'Destroy', post_path(post),
                                    method: :delete,
                                    data: {confirm: 'Are you sure?'} %>
</li>

